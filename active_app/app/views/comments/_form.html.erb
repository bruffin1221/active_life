<%= form_for (@comment) do |f| %>
    <%= f.hidden_field :discussion_id %> 

    <h4>
    <%= f.label :write_comment %><br>
    <%=f.text_area :write_comment, size: "50x10" %>
    </h4>

    <%= f.submit %>

    <% end %>